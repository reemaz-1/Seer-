import 'package:flutter/material.dart';

/// Seer customer app palette (Icon Navy).
class AppColors {
  AppColors._();

  // From the team palette
  static const background = Color(0xFFFFFFFF);
  static const navy = Color(0xFF0E1B33); // header / dark panel
  static const accent = Color(0xFF1C63D6);
  static const cardBorder = Color(0xFFE4E8F0);
  static const textPrimary = Color(0xFF0E1B33);
  static const textSecondary = Color(0xFF6B7385);

  // Extra shades used in the sample design
  static const cardFill = Color(0xFFF4F6FA);
  static const chevron = Color(0xFFB7BECF);
  static const headerText = Color(0xFFF5F8FC);
  static const headerTextMuted = Color(0x99F5F8FC); // 60% opacity
  static const danger = Color(0xFFE4483C);
  static const dangerSoft = Color(0xFFFDECEA);
  static const accentDisabled = Color(0xFF8FB1EA);
}
